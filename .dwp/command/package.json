{
  "name": "@woqso/dwp-command-runtime",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "vitest run",
    "test:unit": "vitest run test/unit",
    "test:integration": "vitest run test/integration",
    "check": "vitest run"
  },
  "devDependencies": {
    "vitest": "^3.2.4"
  }
}
